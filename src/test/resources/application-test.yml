server:
  port: ${ORDER_SERVICE_PORT:8083}

spring:
  datasource:
    url: jdbc:postgresql://localhost:${ORDER_SERVICE_DB_PORT:5435}/${ORDER_SERVICE_DB_NAME:order_db}
    username: ${ORDER_SERVICE_DB_USERNAME:db_username}
    password: ${ORDER_SERVICE_DB_PASSWORD:db_password}

crypto:
  card-secret-key: ${CARD_SECRET_KEY:fF7vJ3wM3zP6qR8sT1uV4xZ6aB9cD2eF3gH6jK9mN1o=}

app:
  user-service-url: http://localhost:8080
  product-service-url: http://localhost:8080
  payment-card-client: http://localhost:8080

logging:
  level:
    root: INFO
